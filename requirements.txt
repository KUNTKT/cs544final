# Core (PyTorch: install the wheel matching your CUDA from https://pytorch.org/ )
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.28.0
safetensors>=0.4.0

# Data & analysis
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0.0
tqdm>=4.66.0
scipy>=1.10.0

# Optional: faster attention on compatible GPUs
# flash-attn>=2.5.0

# Optional: vLLM acceleration (must match CUDA; install per upstream docs)
# vllm>=0.6.0
